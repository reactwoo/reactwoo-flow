You are executing a file-based handoff task for ReactWoo development.

Read these files first:

1. `ai-handoff/current-task.md`
2. `ai-handoff/known-issues.md`

Then:

- Find root cause before patching.
- Change only what the task requires.
- Run the smallest validation listed under Acceptance test.
- Update `ai-handoff/cursor-output.md` when finished (required).

If the same symptom persists after one fix attempt, stop guessing and complete the blocked section in `cursor-output.md`.

Do not continue from chat history alone — the repo files are the source of truth.
